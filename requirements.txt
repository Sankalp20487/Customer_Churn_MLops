# MLflow 2.16.2 Compatible Requirements
# Strategy: Use newer SageMaker container OR force package upgrades

# Core ML/AWS packages - modern versions
sagemaker>=2.200.0
boto3>=1.34.0
botocore>=1.34.0

# Force pandas/numpy upgrade (required for MLflow 2.16.2)
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
scikit-learn>=1.3.0,<2.0.0

# I/O libraries
fsspec>=2024.1.0
s3fs>=2024.1.0

# ML libraries  
xgboost==1.7.6

# MLflow stack - your required versions
mlflow==2.16.2
sagemaker-mlflow==0.1.0

# Utilities
python-dotenv>=1.0.0

# Optional visualization (if needed)
matplotlib>=3.7.0
seaborn>=0.12.0

# Notes:
# 1. This forces pandas 2.x upgrade over container defaults
# 2. You MUST also update your estimator to use framework_version='1.2-1' or newer
# 3. Consider switching to framework_version='1.3-1' in run_training_job.py
# 4. Alternatively, use a custom container (see instructions below)