# Keep compatibility with SageMaker SKLearn 1.2-1 (Python 3.9)
# DO NOT let pip upgrade core libs
# (intentionally NOT listing numpy; use the container's)

# I/O
fsspec==2023.10.0
s3fs==2023.10.0

# Viz (old seaborn so it won't force newer pandas)
seaborn==0.11.2
# don't pin matplotlib; let the container keep its own

# ML extras
xgboost==1.7.6           # lighter CPU build; avoids NCCL pulls
# shap  <-- remove for now (pulls heavy numba/llvmlite)

# MLflow (needed for the ARN tracking URI)
mlflow==2.16.2
sagemaker-mlflow==0.1.0
